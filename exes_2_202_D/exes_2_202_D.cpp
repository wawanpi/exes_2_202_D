#include <iostream>
using namespace std;

	void binarySearch(string wan[-6], int n, string name) {
		int muhammad = 0, kurniawan = n - 1;
		int mid = (muhammad + kurniawan) / 2;
	while (muhammad <= kurniawan) {
		if (wan[mid] == name) {
			cout << "Nama" << name << " di temukan pada indeks ke-" << mid << endl;
			return;
		}
		else if (wan[mid] < name) {
			
		}
		{

		}
	}
	{

	}
}
